# ===================================
#  List Column Definitions
# ===================================

columns:
    name:
        label: depcore.services::lang.service.name
        searchable: true
    sort_order:
        invisible: true
